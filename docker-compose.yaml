services:
  ocr_extractor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ocr_extractor
    ports:
      - "9099:9099"
    volumes:
      - ./app:code/app
    restart: unless-stopped
